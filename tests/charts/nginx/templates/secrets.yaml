{{- if .Values.basicAuth.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "hello-world.fullname" . }}-auth
type: Opaque
data:
  auth: {{ printf "%s:%s" .Values.basicAuth.username .Values.basicAuth.password | b64enc | quote }}
{{- end }}
